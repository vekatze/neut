import {
  core.dictionary {dictionary},
  this.kernel.common.global-name {global-name},
  this.language.common.definite-description {definite-description},
  this.logger.hint {hint},
}

alias top-name-map {
  dictionary(definite-description, pair(hint, global-name))
}
