data log-config {
| Log-Config(
    should-colorize: bool,
    enable-debug-mode: bool,
    enable-silent-mode: bool,
  )
}

constant lsp-log-config: log-config {
  Log-Config{
    should-colorize := False,
    enable-debug-mode := False,
    enable-silent-mode := False,
  }
}
