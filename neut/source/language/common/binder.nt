import {
  sero.sero {Sero, sero},
  this.language.common.ident {ident, ident-sero},
}

data binder-f(m, a) {
| Binder-F(m, ident, a)
}

inline binder-sero<m, a>(s1: sero(m), s2: sero(a)): sero(binder-f(m, a)) {
  let Sero of {put := put-m, get := get-m} = s1;
  let Sero of {put, get} = s2;
  Sero of {
    put := {
      function (k, v) {
        let Sero of {put := put-ident} = ident-sero;
        tie Binder-F(m, x, val) = v;
        put-m(k, m);
        put-ident(k, x);
        put(k, val);
      }
    },
    get := {
      function (k) {
        let Sero of {get := get-ident} = ident-sero;
        try m = get-m(k);
        try x = get-ident(k);
        try val = get(k);
        Right(box {
          letbox m = m;
          letbox x = x;
          letbox val = val;
          Binder-F(m, x, val)
        })
      }
    },
  }
}
