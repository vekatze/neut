import {
  this.language.common.prim-type {prim-type},
}

data low-type {
| Prim-Num(prim-type)
| Pointer
| Array(int, low-type)
| Struct(list(low-type))
| Function(list(low-type), low-type)
| Void
| VarArgs
}
