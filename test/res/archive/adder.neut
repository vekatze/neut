;; adder.neut

(include "./base.neut")

;; make-adder : box i32 -> box (i32 -> i32)

(definition make-adder (x)
  (definition adder (y)
    (core.i32.add (code x) y))
  adder)

(core.i32.print ((make-adder (data 3)) 4))

;; adder.neut ends here.
