;; (include "./string.neut")

(index world)

(definition io
  (state world))

;; (index handle)

;; (primitive stdin (box handle))
;; (primitive stdout (box handle))
;; (primitive stderr (box handle))

;; (let size i32)

;; (let length i32)

;; (index mode
;;   mode.write
;;   mode.read
;;   mode.append
;;   mode.read-write)

;; (primitive open (arrow string mode (io handle)))

;; (primitive close (arrow handle (io top)))

;; (primitive write (arrow string handle size length (io handle)))

;; (primitive read (arrow handle size length (io (tensor handle string))))

(primitive core.i32.print (arrow i32 (io top)))
