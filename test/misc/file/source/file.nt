import {
- core.errno [errno]
- FF
- FM
}

define use-file(): except(errno, unit) {
  try file = open("./test", FF.for-rw, FM.default-file-mode) in
  let _ = write("hey\n", file) in
  let _ = read(file) in
  close(file)
}

define main(): unit {
  let _ = use-file() in
  Unit
}
