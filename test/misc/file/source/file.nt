import {
- core.file.flag => FF
- core.file.mode => FM
}

define main(): unit {
  let file = open("./test", FF.for-rw(), FM.default-file-mode()) in
  let _ = write("hey\n", file) in
  let _ = read(file) in
  close(file)
}
