import {
- this.foo
}

define sample(a: int, some-argument: tau, b: tau): int {
  let _ = some-argument
  let _ = b
  a
}

define main(): int {
  let _ =
    sample {
    - b <= tau
    - a <= 20
    - some-argument <= tau
    }
  let _ =
    foo {
    - a <= 20
    - some-argument <= tau
    - b <= tau
    }
  0
}
