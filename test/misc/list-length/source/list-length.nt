define my-length(a: tau, xs: list(a)): int {
  match xs {
  - Nil =>
    0
  - Cons(_, ys) =>
    add-int(1, my-length(a,  ys))
  }
}

define main(): int {
  let _ = my-length(int, Cons(1, Cons(2, Nil)))
  0
}
