variant my-item(a: tau) {
- Foo()
- Bar()
}

define f(xs: my-item(int), ys: my-item(int)): my-item(int) {
  match xs, ys {
  - Foo(), ys =>
    ys
  - xs, Foo() =>
    xs
  - Bar(), _ =>
    Foo()
  }
}

define main(): int {
  let xs = Bar()
  let ys = Bar()
  let _ = f(xs, ys)
  0
}
