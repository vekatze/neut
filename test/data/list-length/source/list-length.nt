variant list(a: tau) {
- Nil()
- Cons(a, list(a))
}

define length(a: tau, xs: list(a)): i64 {
  match xs {
  - Nil() =>
    0
  - Cons(y, ys) =>
    add-i64(1, length(a,  ys))
  }
}

define main(): i64 {
  length(i64, Cons(1, Cons(2, Nil())))
}
