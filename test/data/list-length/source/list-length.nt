variant list(a: tau) {
- Nil()
- Cons(a, list(a))
}

define length(a: tau, xs: list(a)): i64 {
  match xs {
  - list.Nil() =>
    0
  - list.Cons(y, ys) =>
    add-i64(1, length(a,  ys))
  }
}

define main(): i64 {
  length(i64, list.Cons(1, list.Cons(2, list.Nil())))
}
