variant list(a: tau) {
- Nil()
- Cons(a, list(a))
}

define length(a: tau, xs: list(a)): int {
  match xs {
  - Nil() =>
    0
  - Cons(_, ys) =>
    add-int(1, length(a,  ys))
  }
}

define main(): int {
  length(int, Cons(1, Cons(2, Nil())))
}
