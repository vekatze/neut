use this.cons-arity-mismatch::item

define-data item() =
- cons()
end

define f(xs: item(), ys: item()): item() =
  match xs, ys with
  - cons(), zs ->
    zs
  - cons(x), zs ->
    zs
  end
end

define main(): i64 =
  0
end
