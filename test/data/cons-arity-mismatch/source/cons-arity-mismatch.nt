use
- this.cons-arity-mismatch::item
end

define-data item() with
- cons()
end

define f(xs: item(), ys: item()): item() =
  match xs, ys with
  // - cons(x), zs ->
  //   zs
  - cons(), zs ->
    zs
  end
end

define main(): i64 =
  0
end
