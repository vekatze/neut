use {
- this.cons-arity-mismatch::item
}

data item() {
- cons()
}

define f(xs: item(), ys: item()): item() {
  match xs, ys {
  // - cons(x), zs =>
  //   zs
  - cons(), zs =>
    zs
  }
}

define main(): i64 {
  0
}
