import
- core.bool
end

use
- this.match-unbound::nat
end

define-data nat() with
- zero()
end

define cond(n1: nat()): core.bool::bool() =
  core.bool::bool.false()
end

define f(): nat() =
  let x = zero() in
  if cond(x) then
    zero()
  else
    let _ = x in x
  end
end

define main(): i64 =
  let a = f() in
  0
end
