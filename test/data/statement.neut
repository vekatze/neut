(include "../header.neut")

; statement-metaはquoted扱いになっていて、だから中身がなんか変化していて。
(statement
  (define id
    ((A tau)
     (x A))
    x)
  (reduce (i64.print (id i64 10))))

(reduce (i64.print (id i64 10)))
