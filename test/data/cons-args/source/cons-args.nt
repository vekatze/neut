define-data item(a: tau) as
- cons(a)
end

define view(x: item((_: i64) -> i64)): i64 as
  match x with
  - item.cons(a) ->
    1
  end
end

define main(): i64 as
  view(item.cons(lambda (x). x))
end
