use {
- this.weird-leak::list
}

data list() {
- nil()
- cons()
}

define f(xs: list(), ys: list()): list() {
  match xs, ys {
  - nil(), ys =>
    ys
  - xs, nil() =>
    xs
  - cons(), zs =>
    nil()
  }
}

define main(): i64 {
  let xs = cons()
  let ys = cons()
  let zs = f(xs, ys)
  0
}
