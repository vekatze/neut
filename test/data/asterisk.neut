(ensure core/0.1.0.2
  "https://gitlab.com/veka41/neut-core/-/raw/master/release/0.1.0.1.tar.xz")

(include "core/0.1.0.2/core.neut")

(define (x *) (i64 1))

(define (y *)
  (with identity.bind
    (let (z *) x)
    z))
