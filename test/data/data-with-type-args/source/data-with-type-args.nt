variant list(a: tau) {
- Nil()
- Cons(a, list(a))
}

define f(xs: list(int)): int {
  match xs {
  - Nil() =>
    0
  - Cons(_, _) =>
    1
  }
}

define main(): int {
  f(Cons(10, Cons(20, Nil())))
}
