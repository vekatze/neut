(include "../header.neut")

(record my-record ((a tau))
  (item-1
    a)
  (item-2
    i64)
  (item-3
    top))

(define item
  (my-record.new
    i64
    10
    20
    top.unit))

(reduce (i64.print (my-record.item-1 i64 item)))

(reduce (i64.print (my-record.item-2 i64 item)))

(record empty-record ())
