variant list(a: tau) {
- Nil()
- Cons()
}

define f(xs: list(i64), ys: list(i64)): list(i64) {
  match xs, ys {
  - Nil(), ys =>
    ys
  - xs, Nil() =>
    xs
  - Cons(), _ =>
    Nil()
  }
}

define main(): i64 {
  let xs = Cons()
  let ys = Cons()
  let _ = f(xs, ys)
  0
}
