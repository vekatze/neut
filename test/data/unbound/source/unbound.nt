use {
- this.unbound::list
}

data list(a: tau) {
- nil()
- cons()
}

define f(xs: list(i64), ys: list(i64)): list(i64) {
  match xs, ys {
  - nil(), ys =>
    ys
  - xs, nil() =>
    xs
  - cons(), zs =>
    nil()
  }
}

define main(): i64 {
  let xs = cons()
  let ys = cons()
  let zs = f(xs, ys)
  0
}
