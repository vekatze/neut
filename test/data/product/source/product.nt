struct product(a, b) by Product {
- left: a
- right: b
}

define some-item(x: i64, y: i32, z: i48): i64 * i32 * i48 {
  tuple(x, y, z)
}

define use-tuple(value: i64 * i32 * u64): u64 {
  let tuple(_, _, _) = value
  match value {
  - tuple(_, _, c) =>
    c
  }
}

define main(): i64 {
  0
}
