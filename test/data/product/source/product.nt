struct product(a, b) by Product {
- left: a
- right: b
}

define some-item(x: int, y: i32, z: i48): int * i32 * i48 {
  tuple(x, y, z)
}

define use-tuple(value: int * i32 * i8): i8 {
  let tuple(_, _, _) = value
  match value {
  - tuple(_, _, c) =>
    c
  }
}

define main(): int {
  0
}
