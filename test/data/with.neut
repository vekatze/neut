(include "../header.neut")

(define p
  ((str safe-string))
  (witness i64
    (with identity.bind
      (let _
        (let _ (i64 100))
        (safe-string.print &str)
        (safe-string.print &str))
      (let _ (safe-string.print &str))
      (let len (safe-string.print &str))
      len)))

(reduce (p (safe-string.new "あ")))
