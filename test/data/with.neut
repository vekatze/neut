(define p
  ((str string))
  (witness i64
    (with identity.bind
      (let _
        (let _ (i64 100))
        (string.print &str)
        (string.print &str))
      (let _ (string.print &str))
      (let len (string.print &str))
      (erase str)
      len)))

(p "あ")
