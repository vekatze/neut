(no-implicit-core)

(include "core/notation.neut")

(define functor-form ((m (hom tau tau)))
  (pi
    ((a tau)
     (b tau)
     (_ (hom a b))
     (_ (m a)))
    (m b)))

(record functor ((f (hom tau tau)))
  (map
    (functor-form f)))
