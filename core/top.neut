(no-implicit-core)

(enum top unit)

(include library "core/base.neut")

(define execute ((a tau) (x a))
  (witness top
    (with let-bind
      x
      top.unit)))

;; (attribute execute (implicit 0))
