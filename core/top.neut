(no-implicit-core)

(enum top unit)

(include "core/base.neut")

;; (include "core/identity.neut")

;; (define execute ((a tau) (x a))
;;   (witness top
;;     (with identity.bind
;;       x
;;       top.unit)))

;; (attribute execute (implicit 0))
