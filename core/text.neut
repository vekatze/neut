(no-implicit-core)

(include library "core/foundation.neut")

(definition text-internal ((length u64))
  (array length u64))

(definition text
  (sigma
    ((length u64))
    (text-internal length)))

(section text)

(end text)
