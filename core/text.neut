(no-implicit-core)

(include library "core/foundation.neut")

(define text-internal ((length i64))
  (array length i64))

(define text
  (sigma
    ((length i64))
    (text-internal length)))

(section text)

(end text)
