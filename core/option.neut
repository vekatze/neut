(no-implicit-core)

(include "notation.neut")

(inductive option ((a tau))
  (option:none () (option a))
  (option:some ((x a)) (option a)))

(use option)

(definition option:from-option ([a tau] (x a) (m (option a)))
  (case m
    ((none) x)
    ((some y) y)))

(unuse option)
